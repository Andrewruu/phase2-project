import React from "react";

function Home({isLoggedIn}) {
    return (
        <section id="home">
            <h1>
                Search and Save Your Novels
            </h1>
        </section>
    );
}

export default Home;